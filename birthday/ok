
<!DOCTYPE html>
<html lang="ms">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Happy Birthday, Abang Zubir! 🤘</title>
  <link href="https://fonts.googleapis.com/css2?family=Metal+Mania&family=Oswald:wght@400;600;700&family=Inter:wght@300;400&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --red: #CC0000;
      --red-hot: #FF2200;
      --dark: #080808;
      --charcoal: #111111;
      --steel: #1A1A1A;
      --text: #E0E0E0;
      --muted: #666;
      --silver: #AAAAAA;
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--dark);
      color: var(--text);
      font-family: 'Inter', sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* ── NOISE TEXTURE OVERLAY ── */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
      pointer-events: none;
      z-index: 0;
      opacity: 0.5;
    }

    /* ── LIGHTNING BG ── */
    #bg-canvas {
      position: fixed;
      inset: 0;
      z-index: 0;
      pointer-events: none;
    }

    /* ── HERO ── */
    .hero {
      position: relative;
      z-index: 1;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 2rem 1rem 4rem;
    }

    /* scratched horizontal line */
    .scratch-line {
      width: 120px;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--red), transparent);
      margin: 0 auto 1.2rem;
      opacity: 0;
      animation: fadeIn 0.6s 0.2s ease forwards;
    }

    .eyebrow {
      font-family: 'Oswald', sans-serif;
      font-size: 0.7rem;
      letter-spacing: 0.4em;
      text-transform: uppercase;
      color: var(--red);
      margin-bottom: 1rem;
      opacity: 0;
      animation: fadeIn 0.6s 0.4s ease forwards;
    }

    .hero-title {
      font-family: 'Metal Mania', cursive;
      font-size: clamp(3rem, 11vw, 8rem);
      line-height: 0.95;
      color: #fff;
      text-shadow:
        0 0 20px rgba(204,0,0,0.8),
        0 0 60px rgba(204,0,0,0.4),
        3px 3px 0 #000;
      opacity: 0;
      animation: fadeIn 0.8s 0.6s ease forwards;
    }

    .hero-title span {
      display: block;
      color: var(--red-hot);
      text-shadow:
        0 0 30px rgba(255,34,0,1),
        0 0 80px rgba(255,34,0,0.5),
        3px 3px 0 #000;
    }

    .hero-name {
      font-family: 'Oswald', sans-serif;
      font-size: clamp(1.4rem, 5vw, 2.8rem);
      font-weight: 700;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--silver);
      margin-top: 0.6rem;
      opacity: 0;
      animation: fadeIn 0.8s 0.9s ease forwards;
    }

    /* ── PHOTO FRAME ── */
    .photo-wrap {
      position: relative;
      margin: 3rem auto 0;
      width: clamp(200px, 55vw, 320px);
      opacity: 0;
      animation: fadeIn 1s 1.1s ease forwards;
    }

    /* spiked border SVG ring */
    .spike-ring {
      position: absolute;
      inset: -18px;
      z-index: 3;
      pointer-events: none;
    }

    .photo-frame {
      position: relative;
      z-index: 2;
      width: 100%;
      aspect-ratio: 1;
      border-radius: 50%;
      overflow: hidden;
      border: 4px solid #CC0000;
      box-shadow:
        0 0 0 1px #000,
        0 0 30px rgba(204,0,0,0.6),
        0 0 60px rgba(204,0,0,0.2);
    }

    .photo-frame img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: right center;
      filter: grayscale(30%) contrast(1.15) brightness(0.95);
    }

    /* ── DIVIDER ── */
    .rock-divider {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin: 0 auto 3rem;
      max-width: 300px;
    }
    .rock-divider::before,
    .rock-divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--red));
    }
    .rock-divider::after {
      background: linear-gradient(90deg, var(--red), transparent);
    }
    .rock-divider-icon {
      font-size: 1.4rem;
      filter: drop-shadow(0 0 8px red);
    }

    /* ── MESSAGE SECTION ── */
    .section {
      position: relative;
      z-index: 1;
      max-width: 700px;
      margin: 0 auto;
      padding: 4rem 2rem 5rem;
      text-align: center;
    }

    .message-card {
      background: linear-gradient(160deg, #141414, #0a0a0a);
      border: 1px solid #2A0000;
      border-top: 2px solid var(--red);
      border-radius: 4px;
      padding: 3rem 2.5rem;
      position: relative;
      overflow: hidden;
    }

    /* corner scratch marks */
    .message-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0;
      width: 40px; height: 40px;
      border-top: 2px solid var(--red);
      border-left: 2px solid var(--red);
    }
    .message-card::after {
      content: '';
      position: absolute;
      bottom: 0; right: 0;
      width: 40px; height: 40px;
      border-bottom: 2px solid var(--red);
      border-right: 2px solid var(--red);
    }

    .message-card p {
      font-size: 1rem;
      line-height: 1.9;
      color: #BBBBBB;
      margin-bottom: 1.2rem;
    }
    .message-card p:last-child { margin-bottom: 0; }

    .arabic {
      font-size: 1.4rem;
      color: #CC5555;
      letter-spacing: 0.03em;
      margin-bottom: 1.5rem;
    }

    /* ── WISH CARDS ── */
    .wishes-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 1rem;
      margin-top: 3rem;
    }

    .wish-card {
      background: #0E0E0E;
      border: 1px solid #2A0000;
      border-left: 3px solid var(--red);
      border-radius: 2px;
      padding: 1.4rem 1rem;
      transition: transform 0.25s ease, box-shadow 0.25s ease;
    }

    .wish-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(204,0,0,0.25);
    }

    .wish-icon { font-size: 1.8rem; margin-bottom: 0.6rem; }
    .wish-label {
      font-family: 'Oswald', sans-serif;
      font-size: 0.75rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--red);
      margin-bottom: 0.3rem;
    }
    .wish-text { font-size: 0.85rem; color: #777; line-height: 1.5; }

    /* ── FOOTER ── */
    .footer {
      position: relative;
      z-index: 1;
      text-align: center;
      padding: 2rem 1rem 3rem;
      color: var(--muted);
      font-family: 'Oswald', sans-serif;
      font-size: 0.78rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
    }
    .footer span { color: var(--red); }

    /* ── BIG ROCK SIGN ── */
    .rock-sign {
      font-size: clamp(3rem, 10vw, 6rem);
      margin-top: 1.5rem;
      display: block;
      filter: drop-shadow(0 0 20px rgba(204,0,0,0.9));
      animation: pulse-glow 2s ease-in-out infinite;
      opacity: 0;
      animation: pulse-glow 2s ease-in-out infinite, fadeIn 0.8s 1.4s ease forwards;
    }

    /* ── ANIMATIONS ── */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(16px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    @keyframes pulse-glow {
      0%, 100% { filter: drop-shadow(0 0 16px rgba(204,0,0,0.8)); }
      50%       { filter: drop-shadow(0 0 40px rgba(255,50,0,1)); }
    }

    @keyframes flicker {
      0%,19%,21%,23%,25%,54%,56%,100% { opacity: 1; }
      20%,24%,55% { opacity: 0.4; }
    }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after { animation: none !important; }
    }

    @media (max-width: 480px) {
      .message-card { padding: 2rem 1.2rem; }
    }
  </style>
</head>
<body>

<canvas id="bg-canvas"></canvas>

<!-- HERO -->
<section class="hero">
  <div class="scratch-line"></div>
  <p class="eyebrow">Selamat Hari Jadi &nbsp;·&nbsp; Rock Never Dies</p>

  <h1 class="hero-title">
    Happy<br/>
    <span>Birthday</span>
  </h1>
  <p class="hero-name">🤘 Abang Zubir 🤘</p>
  <span class="rock-sign">🤘</span>

  <div class="photo-wrap">
    <div class="photo-frame">
      <img src="birthday/Abg.jpg" alt="Abang Zubir" />
    </div>
  </div>
</section>

<!-- MESSAGE -->
<section class="section">
  <div class="rock-divider"><span class="rock-divider-icon">🎸</span></div>

  <div class="message-card">
    <p class="arabic">بِسْمِ اللّٰهِ الرَّحْمٰنِ الرَّحِيْمِ</p>
    <p>
      Selamat Hari Jadi, Abang Zubir! 🤘<br/>
      Semoga Allah SWT sentiasa memberkati hidupmu —
      sihat, bahagia, dan penuh semangat macam riff gitar paling kuat
      yang pernah kau dengar.
    </p>
    <p>
      Biarlah hidup kau terus rock macam dirimu —
      tabah, berani, dan tak pernah menyerah.
      Panjang umur, murah rezeki, aamiin! 🤲
    </p>
    <p>
      Terima kasih kerana menjadi abang yang luar biasa.
      Keep it loud. Keep it real. Rock on! 🎸
    </p>
  </div>

  <div class="wishes-grid">
    <div class="wish-card">
      <div class="wish-icon">💪</div>
      <div class="wish-label">Sihat</div>
      <div class="wish-text">Sihat macam rocker sejati</div>
    </div>
    <div class="wish-card">
      <div class="wish-icon">🔥</div>
      <div class="wish-label">Semangat</div>
      <div class="wish-text">Api semangat tak pernah padam</div>
    </div>
    <div class="wish-card">
      <div class="wish-icon">🎸</div>
      <div class="wish-label">Rezeki</div>
      <div class="wish-text">Rezeki melimpah ruah diberkati</div>
    </div>
    <div class="wish-card">
      <div class="wish-icon">🤲</div>
      <div class="wish-label">Doa</div>
      <div class="wish-text">Dipanjangkan umur dalam kebaikan</div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer class="footer">
  Dibuat dengan penuh kasih sayang oleh <span>Adik</span> &nbsp;🤘
</footer>

<script>
  /* ── LIGHTNING / SPARK BG ── */
  const canvas = document.getElementById('bg-canvas');
  const ctx = canvas.getContext('2d');

  function resize() {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
  }
  window.addEventListener('resize', resize);
  resize();

  const particles = [];
  for (let i = 0; i < 60; i++) {
    particles.push({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      vx: (Math.random() - 0.5) * 0.4,
      vy: (Math.random() - 0.5) * 0.4,
      r: Math.random() * 1.5 + 0.3,
      alpha: Math.random() * 0.6 + 0.1,
      color: Math.random() > 0.5 ? '#CC0000' : '#FF2200'
    });
  }

  // Occasional lightning bolt
  let bolts = [];
  function spawnBolt() {
    const x = Math.random() * canvas.width;
    bolts.push({ x, y: 0, life: 1, segments: [] });
    let cx = x, cy = 0;
    while (cy < canvas.height * 0.6) {
      const nx = cx + (Math.random() - 0.5) * 60;
      const ny = cy + Math.random() * 40 + 10;
      bolts[bolts.length - 1].segments.push({ x1: cx, y1: cy, x2: nx, y2: ny });
      cx = nx; cy = ny;
    }
    setTimeout(spawnBolt, Math.random() * 4000 + 3000);
  }
  setTimeout(spawnBolt, 2000);

  function draw() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);

    // particles
    particles.forEach(p => {
      p.x += p.vx; p.y += p.vy;
      if (p.x < 0) p.x = canvas.width;
      if (p.x > canvas.width) p.x = 0;
      if (p.y < 0) p.y = canvas.height;
      if (p.y > canvas.height) p.y = 0;
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fillStyle = p.color.replace(')', `,${p.alpha})`).replace('rgb', 'rgba');
      ctx.fill();
    });

    // lightning bolts
    bolts = bolts.filter(b => b.life > 0);
    bolts.forEach(b => {
      b.life -= 0.04;
      b.segments.forEach(seg => {
        ctx.beginPath();
        ctx.moveTo(seg.x1, seg.y1);
        ctx.lineTo(seg.x2, seg.y2);
        ctx.strokeStyle = `rgba(255,80,0,${b.life * 0.8})`;
        ctx.lineWidth = b.life * 2;
        ctx.shadowBlur = 12;
        ctx.shadowColor = '#FF2200';
        ctx.stroke();
        ctx.shadowBlur = 0;
      });
    });

    requestAnimationFrame(draw);
  }
  draw();
</script>
</body>
</html>
